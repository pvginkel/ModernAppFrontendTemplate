/**
 * Domain-specific test selectors.
 * App-owned — add selectors for your domain pages and components here.
 *
 * Example:
 *   import { testId } from './test-id';
 *
 *   export const itemsSelectors = {
 *     page: testId('items.page'),
 *     list: testId('items.list'),
 *   };
 */
